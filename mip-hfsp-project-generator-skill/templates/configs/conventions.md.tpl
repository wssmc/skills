# 项目交互约定记录

> 本文件记录使用 mip-hfsp-project-generator-skill 生成项目过程中，
> 用户提出的额外约定与偏好。每次新约定都**立即**追加到此文件，
> 作为项目决策的持久化记录，避免对话上下文丢失后无法追溯。

## 生成日期
{{YYYY-MM-DD}}

## 生成者
Skill: `mip-hfsp-project-generator-skill` v{{version}}

---

## 用户明确要求

### 求解器与依赖

- **MIP 求解器**：{Gurobi / 用户指定}
- **Python 版本要求**：{...}
- **禁止使用的库**：{...}
- **其他依赖偏好**：{...}

### 输出格式

- **数值精度**：{目标值保留 X 位，时间保留 Y 位}
- **文件命名约定**：{...}
- **甘特图格式**：{PNG / PDF / 两者都要}
- **日志详细度**：{verbose 默认级别}

### 可视化偏好

- **配色方案**：{学术论文级 / 工程报告级 / 自定义}
- **图片尺寸**：{...}
- **字体**：{...}

### 命名与结构调整

- **项目特定命名**：{如有偏离默认命名规范的约定}
- **目录结构变体**：{如有需要新增/调整的目录}

### 算法与实验偏好

- **默认算法集合**：{是否使用 SA/MA/IG/GA/TS 全部 5 个}
- **消融实验时限**：{是否偏离默认 factor=0.05}
- **批量实验轮数**：{是否偏离默认 7 轮}

### 论文写作偏好

- **期刊目标**：{Elsevier CAS / IEEE / Springer / ...}
- **语言**：{英文 / 中文 / 双语}
- **章节增删**：{是否偏离默认 6 章结构}

### 其他

- {其他用户强调的偏好或要求}

---

## 澄清与决策记录

> 记录对话中，为回答"应该 X 还是 Y？"或"缺失信息如何处理？"这类问题所做的决策。

| 决策点 | 用户选择 | 理由 |
|--------|---------|------|
| {问题} | {答案} | {理由} |

---

## 默认假设确认

> 当问题描述缺少某些细节时，Skill 会给出默认假设。这里记录用户对默认假设的确认或修改。

| 缺失项 | 默认假设 | 用户确认 |
|--------|---------|---------|
| release time | 全部为 0 | ✓ 采用默认 |
| 是否允许等待 | 允许 | ✓ 采用默认 |
| 是否允许抢占 | 不允许 | ✓ 采用默认 |

---

## 约定变更历史

| 日期 | 变更内容 | 原因 |
|------|---------|------|
| {YYYY-MM-DD} | 初始版本 | 项目生成 |

---

## 备注

- 此文件不会被自动覆盖，只会追加。
- 每次交互如有新约定，追加到相应章节。
- 生成项目结束时，`PROJECT_AUDIT.md` 会引用此文件确认约定持久化状态。
