# 根因修复日志（Root-Cause Fix Log）

> 本文件用于记录项目中发生的 bug 及其根因修复过程。
> **严禁**在代码中留下"临时补丁"——所有修复必须记录在此，说明根因与设计变更。
>
> 规则参考：SKILL.md §8「代码质量红线」

---

## 格式

每次修复添加一条记录：

```markdown
### YYYY-MM-DD  {简短标题}

**触发场景**：
{什么条件下出现问题}

**表面症状**：
{观察到的错误信息、异常、错误输出}

**5-Why 根因分析**：
1. 为什么会出现症状 X？→ 因为 A
2. 为什么会出现 A？→ 因为 B
3. 为什么会出现 B？→ 因为 C
4. 为什么会出现 C？→ 因为 D
5. 为什么会出现 D？→ **根因：{设计/逻辑/接口问题}**

**根因位置**：
{文件路径 + 行号 + 具体逻辑}

**修复方式**：
- 修改 `{file}` 的 `{function}`：{描述}
- 同步更新调用方 `{file}`：{描述}
- 更新接口契约：{如函数签名/返回值语义变更}

**为什么不是打补丁**：
{说明为什么这个修复触及根因，而非局部规避}

**回归测试**：
- `tests/{test_file}`：新增 {test_case}，覆盖该场景

**影响范围**：
{列出被此变更影响的模块/文件}
```

---

## 修复记录

### {{YYYY-MM-DD}}  初始版本

**触发场景**：项目生成

**说明**：本文件初始化，尚无修复记录。

---

## 允许的例外（不视为补丁）

以下情形允许"看起来像补丁"的代码，但**必须**在此文件登记：

### 上游库 bug

| 日期 | 位置 | 上游 bug 链接 | 计划移除时机 |
|------|------|--------------|-------------|
| — | — | — | — |

### 数值稳定性护栏

| 日期 | 位置 | 误差来源 | 容差 |
|------|------|---------|------|
| — | — | — | — |

> **注意**：**不再允许"向后兼容"作为例外**（SKILL.md §8.2）。
> 任何接口/格式变更必须一次性迁移全部调用点，禁止保留旧接口 shim。
