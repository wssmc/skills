# General Python utilities. The optional IBM `cplex` package is configured
# separately because its availability and license depend on the CPLEX install.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scipy>=1.10
